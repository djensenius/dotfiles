on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
    with:
      build_matrix_path: keyboards/pepito_v1.13/build.yaml
      config_path: keyboards/pepito_v1.13/config
