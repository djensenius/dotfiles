---
name: server
root: ~/

windows:
  - Monitor:
      layout: main-horizontal
      panes:
        - btm
        - tmux set-window-option -t1 automatic-rename on && ctop
        - ~/padd.sh
  - Code: tmux set-window-option -t2 automatic-rename on && nvim
