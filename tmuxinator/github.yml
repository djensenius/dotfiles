---
name: github
root: /workspaces/github

windows:
  - Code:
      layout: main-vertical
      panes:
        - tmux set-window-option -t1 automatic-rename on && nvim
  - Server: tmux set-window-option -t1 automatic-rename on && script/server --debug
